{
  "name": "chrona",
  "version": "1.0.0",
  "private": true,
  "description": "Track where your time actually goes — tasks, habits, and every hour of your day.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "icons": "node tools/make-icons.js",
    "build": "node tools/build-www.js",
    "android:add": "npm run build && npx cap add android",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
